// bool = 1 byte because 
// 1 bit is sufficient but we cannot address 1 bit
// we need at least 1 byte to make it addressable using variable name

// 1 byte = 8 bit
// float = 4 byte ==> for small numbers
// double = 8 byte

// int = 4 byte = 32 bits => 2^32 - 1 = is becoming the max number that we can store into int

// char = 1 byte = 8 bit = 2^8 - 1 = 255 max can store to 255
// char name = 256 // overflow ho jayega

// sizeof(varName) -- gives u size in bytes
